<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Satisfy&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant:wght@300&display=swap" rel="stylesheet">
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css" />
    <title><%= tabTitle %></title>
    <style></style>
  </head>
  <body>
    <div class='container'>
        <header class="navbar">
            <img class="head-pic" src="/img/birgith-roosipuu-PWPe-x1Ygtw-unsplash.jpg">
            <h4>For those of us who are forgetful</h4>
            <nav>
                <% if (currentUser) {%>
                    <form action="/logout?_method=DELETE" method="post">
                        <input class="button button-secondary" type="submit" value="Log Out" />
                    </form>
                <% } else { %>
                    <a class="button button-secondary" href="/login">Log In</a>
                <% } %>
            </nav>
        </header>
     


<div class="form-container">
  <h1>Your Personal Skincare Journal</h1>
  <br />
  <h2>Log In</h2>

  <div class="flash-messages">
    <% if (messages.success) { %> <% for (let msg of messages.success) { %>
    <p class="success"><%= msg %></p>
    <% } %> <% } %> <% if (messages.info) { %> <% for (let msg of messages.info)
    { %>
    <p class="info"><%= msg %></p>
    <% } %> <% } %> <% if (messages.error) { %> <% for (let msg of
    messages.error) { %>
    <p class="error"><%= msg %></p>
    <% } %> <% } %>
  </div>

  <form action="<%= baseUrl %>/login" method="post">
    <p>
      <label for="username">Username</label>
      <input type="text" name="username" id="username" required />
    </p>
    <p>
      <label for="password">Password</label>
      <input type="password" name="password" id="password" required />
    </p>

    <input class="button button-primary" type="submit" value="Login" />

    <p>
      Don't have an account yet?
      <a href="/users/signup">Sign up here</a>
    </p>
  </form>
</div>
  </body>
</div>
</html>
<%- include('../partials/footer.ejs') %>
